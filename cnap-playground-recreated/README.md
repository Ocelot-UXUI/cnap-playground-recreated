# CNAP Playground Recreated

基于已登录的 CNAP 组件视觉规范 Playground 页面重建的独立前端演示项目。

## 运行

无需安装依赖，直接在项目目录运行：

```bash
python3 -m http.server 4173
```

然后打开 <http://localhost:4173>。

## 范围

项目包含 Playground 的页面框架、左侧组件导航、Button 组件的尺寸/类型/禁用状态示例，以及其余组件的可交互占位示例。原页面未提供源码导出，因此这是依据可见 UI 和交互重建的前端实现，不包含原项目后端或内部依赖。
